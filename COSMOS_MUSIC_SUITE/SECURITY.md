# Security

Do not open a public issue containing API keys, signing material, private sensor data or an exploit that would put users at immediate risk. Revoke any accidentally committed secret before discussing it. The application should remain functional without cloud credentials; secrets must never be embedded in browser JavaScript.

Security-sensitive changes should preserve the local-first default and permission-on-demand model.
